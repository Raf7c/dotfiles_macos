- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work

- shell:
    - sleep 2
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true