- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/bat:
        create: true
        path: config/bat

- create:
    - ~/Work

- shell:
    - sleep 2
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true